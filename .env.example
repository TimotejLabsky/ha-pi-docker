# Copy to .env and adjust. Both are optional.

# Where Home Assistant keeps its configuration, database and backups.
# Relative to this directory, or an absolute path (e.g. a USB SSD mount).
HA_CONFIG_DIR=./config

# Container time zone (affects automations, logs).
TZ=Europe/Bratislava
